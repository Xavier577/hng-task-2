development:
  driver: postgres
  open: $DATABASE_URL dbname=$PG_DATABASE sslmode=$PG_SSL_MODE

production:
  driver: postgres
  open: $DATABASE_URL dbname=$PG_DATABASE sslmode=$PG_SSL_MODE

#customimport:
#  driver: postgres
#  open: postgres open
#  import: gorm.io/driver/postgres
#  dialect: postgres

environment_variable_config:
  driver: postgres
  open: $DATABASE_URL
